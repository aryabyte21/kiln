babel_version: "1.0"

tool:
  id: com.aria.tools.url_extractor
  name: url_extractor
  version: 1.0.0
  description: Extract direct URLs from a list of web search results based on a specific pattern or file type (e.g., CSV).
  author: vibe_tool

interface:
  inputs:
    - name: search_results
      type: object
      description: A dictionary containing web search results with titles, links, and snippets.
      required: true
    - name: file_type
      type: string
      description: The file type to filter URLs by (e.g., 'csv', 'pdf').
      required: true
  outputs:
    - name: url
      type: string
      description: The direct URL to the file matching the specified file type.

implementation:
  runtime: python3.10
  entrypoint: impl.py
  dependencies: []

testing:
  fixtures:
    - input:
        search_results:
          items:
            - link: "https://example.com/data.csv"
            - link: "https://example.com/document.pdf"
        file_type: "csv"
      expected_output_contains:
        - url

metadata:
  tags: [synthesized]
  category: general
  generated_by: vibe_tool
