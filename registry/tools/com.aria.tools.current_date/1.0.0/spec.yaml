babel_version: "1.0"

tool:
  id: com.aria.tools.current_date
  name: current_date
  version: 1.0.0
  description: Fetch the current date and time in a specified format.
  author: vibe_tool

interface:
  inputs:
    - name: format
      type: string
      description: The desired output format for the date (e.g., 'YYYY-MM-DD')
      required: false
      default: null
  outputs:
    - name: date
      type: string
      description: The current date in the specified format.
    - name: timestamp
      type: integer
      description: The current Unix timestamp.

implementation:
  runtime: python3.10
  entrypoint: impl.py
  dependencies: []

testing:
  fixtures:
    - input:
        format: "YYYY-MM-DD"
      expected_output_contains:
        - date
        - timestamp

metadata:
  tags: [synthesized]
  category: general
  generated_by: vibe_tool
