babel_version: "1.0"

tool:
  id: com.aria.tools.friendly_response_generator
  name: friendly_response_generator
  version: 1.0.0
  description: Generate a friendly and contextually appropriate response based on conversation context and user sentiment.
  author: vibe_tool

interface:
  inputs:
    - name: context
      type: object
      description: The conversation context, including user sentiment and intent.
      required: true
  outputs:
    - name: response
      type: string
      description: A friendly and contextually appropriate response to the user.

implementation:
  runtime: python3.10
  entrypoint: impl.py
  dependencies: []

testing:
  fixtures:
    - input:
        context: {}
      expected_output_contains:
        - response

metadata:
  tags: [synthesized]
  category: general
  generated_by: vibe_tool
