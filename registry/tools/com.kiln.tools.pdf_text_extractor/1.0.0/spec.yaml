kiln_version: "1.0"

tool:
  id: com.kiln.tools.pdf_text_extractor
  name: pdf_text_extractor
  version: 1.0.0
  description: Extract plain text and structured financial data (e.g., revenue, net income) from SEC filing PDFs.
  author: vibe_tool

interface:
  inputs:
    - name: pdf_url
      type: string
      description: URL of the SEC filing PDF to extract text from.
      required: true
  outputs:
    - name: text_content
      type: string
      description: Extracted plain text from the PDF.
    - name: financial_data
      type: object
      description: Structured financial data (e.g., revenue, net income, EPS) extracted from the report.

implementation:
  runtime: python3.10
  entrypoint: impl.py
  dependencies: []

testing:
  fixtures:
    - input:
        pdf_url: "test"
      expected_output_contains:
        - text_content
        - financial_data

metadata:
  tags: [synthesized]
  category: general
  generated_by: vibe_tool
