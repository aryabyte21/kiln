babel_version: "1.0"

tool:
  id: com.aria.tools.user_profile_inferrer
  name: user_profile_inferrer
  version: 1.0.0
  description: Infer or retrieve the user's professional profile, including skills, experience, and preferences, to match against job postings.
  author: vibe_tool

interface:
  inputs:
    - name: user_id
      type: string
      description: Unique identifier for the user.
      required: true
  outputs:
    - name: skills
      type: array
      description: List of user's skills.
    - name: experience
      type: array
      description: List of user's past job experiences with titles, companies, and durations.
    - name: preferences
      type: object
      description: User's job preferences, including industry, role type, and location.

implementation:
  runtime: python3.10
  entrypoint: impl.py
  dependencies: []

testing:
  fixtures:
    - input:
        user_id: "test"
      expected_output_contains:
        - skills
        - experience
        - preferences

metadata:
  tags: [synthesized]
  category: general
  generated_by: vibe_tool
