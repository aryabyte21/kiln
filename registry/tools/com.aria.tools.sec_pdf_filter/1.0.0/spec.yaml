babel_version: "1.0"

tool:
  id: com.aria.tools.sec_pdf_filter
  name: sec_pdf_filter
  version: 1.0.0
  description: Filters SEC filings to identify those available as PDFs and extracts their download URLs.
  author: vibe_tool

interface:
  inputs:
    - name: filings
      type: object
      description: A list of SEC filings fetched from the SEC filings fetcher tool.
      required: true
  outputs:
    - name: pdf_urls
      type: array
      description: List of URLs pointing to the PDF versions of the filings.

implementation:
  runtime: python3.10
  entrypoint: impl.py
  dependencies: []

testing:
  fixtures:
    - input:
        filings: {}
      expected_output_contains:
        - pdf_urls

metadata:
  tags: [synthesized]
  category: general
  generated_by: vibe_tool
