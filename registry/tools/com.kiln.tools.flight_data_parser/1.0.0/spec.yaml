kiln_version: "1.0"

tool:
  id: com.kiln.tools.flight_data_parser
  name: flight_data_parser
  version: 1.0.0
  description: Parse plain-text or HTML content from flight listing websites to extract structured flight details such as airline, departure/arrival times, prices, and duration.
  author: vibe_tool

interface:
  inputs:
    - name: content
      type: string
      description: The plain-text or HTML content fetched from a flight listing website.
      required: true
    - name: origin
      type: string
      description: The origin city or airport code (e.g., Singapore, SIN).
      required: true
    - name: destination
      type: string
      description: The destination city or airport code (e.g., India, DEL).
      required: true
  outputs:
    - name: flights
      type: array
      description: A list of flights, each containing structured details.

implementation:
  runtime: python3.10
  entrypoint: impl.py
  dependencies: []

testing:
  fixtures:
    - input:
        content: "test"
        origin: "test"
        destination: "test"
      expected_output_contains:
        - flights

metadata:
  tags: [synthesized]
  category: general
  generated_by: vibe_tool
