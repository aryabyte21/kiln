kiln_version: "1.0"

tool:
  id: com.kiln.tools.movie_parser
  name: movie_parser
  version: 1.0.0
  description: Parse plain-text content from movie listing websites to extract structured details such as movie titles, showtimes, theatre locations, and ratings.
  author: vibe_tool

interface:
  inputs:
    - name: content
      type: string
      description: The plain-text content fetched from a movie listing website.
      required: true
    - name: location
      type: string
      description: The location or country for which the movie listings are relevant (e.g., 'Singapore').
      required: true
  outputs:
    - name: movies
      type: array
      description: A list of movies, where each movie is represented as a dictionary.

implementation:
  runtime: python3.10
  entrypoint: impl.py
  dependencies: []

testing:
  fixtures:
    - input:
        content: "test"
        location: "test"
      expected_output_contains:
        - movies

metadata:
  tags: [synthesized]
  category: general
  generated_by: vibe_tool
