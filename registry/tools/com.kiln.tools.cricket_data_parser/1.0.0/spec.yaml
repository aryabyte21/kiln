kiln_version: "1.0"

tool:
  id: com.kiln.tools.cricket_data_parser
  name: cricket_data_parser
  version: 1.0.0
  description: Parse plain-text or HTML content from cricket websites to extract structured match details such as live scores, teams, players, match status, and commentary.
  author: vibe_tool

interface:
  inputs:
    - name: content
      type: string
      description: Plain-text or HTML content fetched from a cricket website.
      required: true
  outputs:
    - name: matches
      type: array
      description: List of ongoing or recent matches with details.
    - name: match_details
      type: object
      description: Structured details for each match, including teams, scores, players, status, and commentary.
    - name: live_updates
      type: array
      description: List of key events or live updates from ongoing matches.

implementation:
  runtime: python3.10
  entrypoint: impl.py
  dependencies: []

testing:
  fixtures:
    - input:
        content: "test"
      expected_output_contains:
        - matches
        - match_details
        - live_updates

metadata:
  tags: [synthesized]
  category: general
  generated_by: vibe_tool
