Before implementing any feature, look up the latest documentation for the relevant technologies using Context7 MCP.

Use these library IDs:

- OpenClaw: `/openclaw/openclaw` or `/llmstxt/openclaw_ai_llms-full_txt` (full docs)
- NATS Go: `/nats-io/nats.go`
- NATS Docs: `/nats-io/nats.docs`
- React Flow: `/xyflow/web`
- Next.js: `/vercel/next.js`
- pgx (Go PostgreSQL): `/jackc/pgx`
- go-redis: `/redis/go-redis`
- Cobra CLI: `/spf13/cobra`

Query the docs for: $ARGUMENTS

Use `mcp__plugin_context7_context7__query-docs` with the appropriate libraryId and the query above. If multiple technologies are relevant, query them all in parallel.

Summarize the key findings, code patterns, and any gotchas relevant to the current task.
