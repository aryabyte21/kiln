locals {
  name_prefix = "${var.project_name}-${var.environment}"
}

# Placeholder for core platform resources.
# Start by adding VPC, EKS (or managed container runtime), and observability primitives here.
